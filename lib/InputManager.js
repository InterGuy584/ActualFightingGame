import { keys } from "../src/globals.js";

export default class InputManager
{
    static buttons = [
        'enter',
        'w', 'a', 's', 'd'
    ];

    constructor() {
        this.controls = {};
    }

    checkLowercaseKeys() {
        
    }
}